# PasswordVault

獨立的密碼管理應用程式。核心是本機、零網路的密碼庫（`PasswordVault.Core`），這輪規劃新增可選的雲端同步能力，讓同一使用者能跨裝置共用同一份密碼庫。

## Language

**帳號密碼（Account Password）**：
雲端同步登入用的密碼，伺服器存雜湊（Argon2id）並負責驗證，可以做「忘記密碼」重設。跟「主密碼」是兩個完全獨立、不能互相推導的東西——這個分離是雲端同步零知識架構最核心的一條線，見 [`雲端同步_功能規劃.md`](docs/specs/features/雲端同步_功能規劃.md) 第 2 節。
_Avoid_: 直接用「密碼」不加區分——這個專案裡「密碼」在不同語境下可能指主密碼、帳號密碼、或密碼庫裡存的某一筆網站密碼，三者都不同，混用會讓人搞不清楚在講哪一個。

**主密碼（Master Password）**：
本機解密密碼庫內容用的密碼，衍生金鑰後解密實際憑證——雲端同步的伺服器全程看不到、也驗證不了這個密碼，忘記了無法復原（沒有後門）。不會因為加了雲端同步而改變這個既有的安全承諾。

**零知識架構（Zero-Knowledge Architecture）**：
雲端同步的核心設計原則：伺服器只能存放/搬移「加密過的內容」，不能存放/推導出任何能解密內容的東西（不存主密碼、不存衍生金鑰、不做伺服器端解密）。所有實際加解密運算都在客戶端完成。見 [`雲端同步_功能規劃.md`](docs/specs/features/雲端同步_功能規劃.md)。
